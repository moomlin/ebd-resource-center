# Netlify 部署指南

## 📋 前置準備

1. **建立 GitHub 帳號**（如果還沒有）
   - 前往 https://github.com
   - 註冊新帳號

2. **建立 Netlify 帳號**
   - 前往 https://app.netlify.com
   - 用 GitHub 帳號登入（推薦）

## 🚀 部署步驟

### 步驟 1：初始化 Git 倉庫

在您的本地機器上，進入專案資料夾，執行：

```bash
cd /Users/paul890401957/Desktop/00_練習做網站

# 初始化 git 倉庫
git init

# 添加所有檔案
git add .

# 建立第一個提交
git commit -m "Initial commit: 情支資源中心網站"
```

### 步驟 2：上傳到 GitHub

1. **建立新的 GitHub Repository**
   - 前往 https://github.com/new
   - 名稱：`ebd-resource-center`（或您偏好的名稱）
   - 選擇 "Public"
   - 不要初始化 README、.gitignore 或 license
   - 點擊 "Create repository"

2. **連接本地倉庫到 GitHub**

```bash
# 將本地倉庫連接到 GitHub
# 將 YOUR_USERNAME 替換為您的 GitHub 帳號名稱
git remote add origin https://github.com/YOUR_USERNAME/ebd-resource-center.git

# 重命名預設分支為 main（如果還沒有）
git branch -M main

# 推送到 GitHub
git push -u origin main
```

### 步驟 3：在 Netlify 上部署

1. **登入 Netlify**
   - 前往 https://app.netlify.com
   - 用 GitHub 帳號登入

2. **連接新網站**
   - 點擊 "New site from Git"
   - 選擇 "GitHub"
   - 授予 Netlify 存取 GitHub 的權限
   - 搜尋並選擇 `ebd-resource-center` 倉庫
   - 點擊 "Deploy"

3. **配置設定**
   - Build command: 留空（我們沒有建置步驟）
   - Publish directory: `.`（根目錄）
   - Netlify 應該已經偵測到 `netlify.toml` 配置

4. **完成部署！**
   - Netlify 會自動建置並部署您的網站
   - 您將獲得一個免費的 `.netlify.app` 域名

### 步驟 4：後續更新

每次您想更新網站時，只需：

```bash
# 在本地進行變更
# ...修改檔案...

# 添加變更
git add .

# 提交變更
git commit -m "描述您的變更"

# 推送到 GitHub
git push
```

Netlify 會自動偵測到 GitHub 的推送，並重新部署您的網站！

## 🔧 自訂域名

如果您想使用自訂域名（例如 `yourschool.com`）：

1. 在 Netlify 上：
   - 進入網站設定
   - 選擇 "Domain management"
   - 點擊 "Add custom domain"
   - 輸入您的域名

2. 按照 Netlify 的指示配置 DNS 設定

## ⚙️ 環境變數（如需要）

如果您需要使用環境變數（例如 Firebase 配置）：

1. 在 Netlify 儀表板上：
   - 進入 "Site settings"
   - 選擇 "Build & deploy" → "Environment"
   - 添加環境變數

2. 在您的程式碼中透過 `import.meta.env.VITE_VAR_NAME` 存取

## 📞 常見問題

**Q: 我可以免費使用 Netlify 嗎？**
A: 是的！Netlify 提供免費的靜態網站託管，無需信用卡。

**Q: 部署需要多長時間？**
A: 通常 1-3 分鐘。

**Q: 我可以回滾到舊版本嗎？**
A: 是的，Netlify 保留所有版本歷史，您可以隨時回滾。

**Q: 我可以自訂網站 URL 嗎？**
A: 可以。預設 URL 為 `[random-name].netlify.app`，您可以在設定中變更。

## 🔐 安全性提醒

⚠️ **重要：Firebase 配置密鑰**

您的 Firebase 配置已經公開在 HTML 檔案中。這在測試環境中是可以接受的，但在生產環境中：

1. 啟用 Firebase 安全規則以保護資料庫
2. 考慮使用環境變數（需要建置工具）
3. 限制 API 密鑰的使用範圍

檢查您的 Firestore 安全規則設定：
https://console.firebase.google.com → 您的專案 → Firestore Database → Rules

## 📝 更新紀錄

每次推送更新時，確保：
- 所有檔案已保存
- 沒有語法錯誤
- 本地測試正常
- 提供清晰的 commit 訊息

---

需要幫助？聯繫 Netlify 支援：https://support.netlify.com
